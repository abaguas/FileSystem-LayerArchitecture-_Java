/*package pt.tecnico.mydrive.service;

public class LoginTest extends AbstractServiceTest{
	protected void populate(){

		MyDrive md = MyDrive.getInstance();
		User u1= new User("Super User", "***", "root");
		Session s1 = new Session (u1, md);

		User u2 = new User ("Super User", "****", "root");
		Session s2= new Session (u2, md);
		
		User u3 = New User ("Non Existent", "****", "NonExistent");
		Session s3= new Session(u3, md);
		
		Session s4 = new Session(u1, md);
		int time = s4.getTime().getTimestamp();
		if time != 0 ->Erro 
		
		Session s5 = new Session (u1, md);
		int time = s5.getTime().getTimestamp();
		if time != 0 ->Erro 

		Session s6 = new Session (u1, md);
		int time = s6.getTime().getTimestamp();
		


	@Test
	public void SuccessfulLogin(){
		 //Sucesso
	}
	
	@Test(expected = InvalidUserOrPasswordException.class)
		public void UnsuccessfulLoginDueToWrongPassword(){
 //Erro
		}
	
	@Test(expected = InvalidUserOrPasswordException.class)
		public void UnsuccessfulLoginDueToNonExistentUser(){
//Erro
		}
	
	
	@Test
	public void initiateSessionPeriod(){
	}
	
	@Test
	public void reinitiateSessionPeriod(){
}
*/
	
		
